# Banco de dados
DB_HOST=postgres
DB_PORT=5432
DB_NAME=configpanel
DB_USER=
DB_PASSWORD=

# Autenticação JWT
JWT_SECRET=
JWT_EXPIRES_IN=8h

# Servidor
PORT=3001

# E-mail (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=